{
  "name": "nodejs-broker",
  "version": "0.1.0",
  "description": "nodejs broker is an operation broker that narrows down multiple operations of the same type being executed to a single operation, while an operation is under execution every other operation of the same type blocks on that operation pending for its results",
  "main": "lib/broker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "operation",
    "broker"
  ],
  "author": "Saad Talaat",
  "license": "GPL",
  "dependencies": {
    "coffee-script": "^1.10.0"
  }
}
