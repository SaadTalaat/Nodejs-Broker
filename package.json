{
  "name": "nodejs-broker",
  "version": "0.1.5",
  "description": "nodejs broker is an operation broker that narrows down multiple operations of the same type being executed to a single operation, while an operation is under execution every other operation of the same type blocks on that operation pending for its results",
  "main": "lib/broker.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --compilers coffee:coffee-script/register --require coffee-coverage/register-istanbul test/"
  },
  "keywords": [
    "nodejs",
    "operation",
    "broker"
  ],
  "author": "Saad Talaat",
  "license": "GPL",
  "devDependencies": {
    "assert": "^1.3.0",
    "coffee-coverage": "^1.0.1",
    "coffee-script": "^1.10.0",
    "istanbul": "^0.4.4",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "promise": "^7.1.1"
  },
  "repository":{
      "url":"git://github.com/SaadTalaat/Nodejs-Broker.git"
  }
}
